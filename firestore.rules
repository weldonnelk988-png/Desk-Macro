rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Accès réservé aux utilisateurs connectés (voir Authentication dans la console Firebase).
    match /deskmacro/{docId} {
      allow read, write: if request.auth != null;
    }
  }
}
